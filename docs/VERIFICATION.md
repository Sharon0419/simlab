# 首版本机交付验证

## 0.4.0 固定时段值守交付（2026-09-08）

最终核心测试为 **69 passed in 3.75s**。新增固定终点、补位准备与取消、最低保障与目标数量区分、优先级不抢占、连续不达标跨采样、容忍边界、规则输入拒绝、按日追加及冲突、共享剖面隔离、旧项目版本升级、未知扩展版本拒绝、结果可复现和交换往返测试。原52项回归保留。

最终源码 `build/qa-v04-source-release/smoke-result.json` 与最终原位置可执行版 `build/qa-v04-package-release/smoke-result.json` 均 **passed**、退出码 **0**，**27项桌面流程检查通过**。新增中文值守示例、规则保存、按日预览/冲突、编辑使预览失效、真实后台计算与新指标、任务CSV和完整结果交换。已检查源码值守计划和最终便携版不达标时段截图，深蓝界面中文正常。

验收使用多层维修示例，原任务最低数量改14、补位0.75小时，新增前两天10–12点目标2/最低1的重点窗口，运行3次重复。源码和便携版最低保障达标率均 **0.8887729732760024**，窗口合格率 **0.8020833333333334**，平均补位缺口 **25.258365344153574设备小时**。

旧连续基准可用度仍为 **0.9596487296178109**，旧任务基准满足率仍为 **0.9013327788438158**，旧多层验收基准可用度仍为 **0.8927794709312838**。合成数据仅用于验证当前规则，不是原厂对标或实际装备预测。

最终程序为 `dist/SimLab/SimLab.exe`，原启动脚本不变；v0.3程序保留在 `dist/SimLab-backup-v0.3.0`。用户项目目录未迁移。便携包另含连续、任务、多层维修、固定值守四套模型/结果示例，共8个项目包。

## 0.3.0 多层维修交付（2026-09-07）

核心测试最终结果为 **52 passed in 1.75s**。保留旧版本回归，新增无备件解析基准、备用 LRU 提前恢复、备用 SRU 独立周转、共享资源排队、跨班等待、未完成维修统计、健康子件剩余故障预算、非法层级拦截及实物部件守恒检查。

源码 `build/qa-v03-source/smoke-result.json` 和原安装位置的最终便携版 `build/qa-v03-package/smoke-result.json` 均为 **passed**、退出码 **0**，全部 **20 项桌面流程检查通过**。包含新建多层示例、组成树、维修工序编辑、后台仿真、维修 CSV 和带结果项目交换。已查看最终便携版深蓝工序耗时界面截图 `build/qa-v03-package/08-maintenance-results.png`。

多层验收示例修改检测时间为 2 小时、运行 3 次重复，源码与便携版可用度均为 **0.8927794709312838**，已完成 LRU 平均周转时间 **101.6490102510211 小时**。旧连续示例仍为 **0.9596487296178109**，任务验收示例满足率仍为 **0.9013327788438158**。这些合成场景用于回归验证，不是实际装备预测或原厂对标结论。

新版安装位置保持 `dist/SimLab/SimLab.exe`，原启动脚本无需修改。旧版文件夹保存在 `dist/SimLab-backup-v0.2.0`；用户项目目录未迁移。构建脚本在清理目标目录前检查该位置程序是否正在运行。

## 0.2.0 任务与班次交付（2026-09-07）

核心测试结果为 **36 passed in 1.08s**。覆盖固定需求窗口供需积分、部署不足、重叠竞争、同刻交接、任务终点、故障补位、运行时钟跨待命保留、资源班次门控、跨班作业、共同班次检查、非法规则拦截、长日历浮点余量回归和旧引擎基准。基准说明见 V0.2.md。

源码 `build/qa-v02-source-final/smoke-result.json` 与最终便携版 `build/qa-v02-package/smoke-result.json` 均 **passed**、退出码 **0**，14 项桌面流程检查通过。包括旧模式的 8 项检查和任务示例入口、任务字段编辑、后台任务仿真、结果持久化、任务 CSV、含任务结果项目交换。已检查最终便携版深蓝任务结果截图。

合成验收场景：20 台设备，30 天每天需求 22 台、8 小时，3 次重复；设备小时满足率 **0.9013327788438158**，平均缺口 **520.9629277046521 设备小时**。其中部署数量不足造成的缺口为手算的 **480 设备小时**，其余来自实际故障保障过程。源码版与便携版一致。旧连续模式示例可用度仍为 **0.9596487296178109**。

这些检查验证当前明确定义的子集，不替代真实数据校准或原厂数值对标。

## 深蓝主题更新（2026-09-07）

统一调整背景、面板、按钮、表格、字段说明和自绘图表颜色。重新构建 Windows 可执行版，运行 `--smoke build/qa-deep-blue-package`，退出码 0，八项桌面检查 passed；合成示例可用度仍为 0.9596487296178109。已检查建模与结果截图的中文和深蓝配色。

## 2026-09-07 复验

本次重新执行核心测试，结果为 **14 passed in 1.74s**。源码及现有 Windows 便携版分别执行端到端检查，均退出 0、状态 passed，八项检查全部通过。证据位于 `build/qa-20260907-source/smoke-result.json` 与 `build/qa-20260907-package/smoke-result.json`；本次合成示例的平均可用度为 0.9596487296178109，与两种运行方式一致。下文为首次构建记录。

验证日期：2026-09-04。软件版本：0.1.0。

## 字段基线

依据本机 SIMLOX 2017 安装目录内 DatabaseDictionary_SIMLOX.txt 生成。133 张表、891 个字段的代码、顺序、说明、类型、单位、默认值、约束与关联均逐项比对一致；源文件 SHA256 与 schema.json 中记录一致。

## 核心测试

命令：`.venv/Scripts/python.exe -m pytest -q`

结果：**14 passed**。

覆盖：字典数量与关键单位、示例引用关系、每百万小时故障率换算、非法数值、悬空引用与重复键、未支持参数拦截、SQLite 往返与覆盖备份、项目包导入分支、路径穿越与篡改拒绝、模型包不含结果、可复现、零故障、部件守恒、无备件单机解析基准、不可满足的资源组合拒绝。

## 桌面端到端验证

源码版：`build/qa-source-3/smoke-result.json`。

最终便携版：`dist/SimLab/SimLab.exe --smoke build/qa-release`，进程退出码 **0**，结果 **passed**。

通过八项检查：字段编辑、保存重开、CSV 导出、后台取消、后台计算、结果持久化、项目包导出导入、分支追溯。

样本为修改动力模块 FRT=1300 后的 20 台设备、90 天、10 次重复试验。源码版与便携版的平均可用度均为 **0.9596487296178109**。此数值来自合成数据，不代表真实装备指标。

完整流程截图位于 `build/qa-release/01-overview.png`、`02-modeling.png`、`03-results.png`，并复制模型与结果预览至本目录。已检查中文渲染、导航、表头、字段说明、结果卡片、曲线与停机分析表。

## 交付边界

已实现本机基础保障仿真与全量字段编辑。未声称复现原厂引擎；高级业务规则和原厂 .sxi 二进制文件尚不支持，界面与 README 已明确说明。仅在本机 Windows 11 完成执行验证。

打包时发现 PATH 上的 ICU 78 被错误收集，与 Qt 需要的 Windows ICU 未版本化入口不匹配；构建脚本已排除该库，并直接对修正后的最终可执行程序完成以上测试。
