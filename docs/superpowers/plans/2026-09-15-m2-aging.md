# M2 Aging Implementation Plan

> **For agentic workers:** Use executing-plans to implement this plan task-by-task. Preserve the existing uncommitted release work.

**Goal:** Deliver the approved M2 aging design with physical identity, repair age updates, Chinese inputs/results, regression evidence and a Windows release.

**Architecture:** Keep the legacy simulation path unchanged for models without aging. Add a stable cumulative-hazard primitive and use physical component scheduling for enabled models. Settle running age at assignment boundaries and the horizon.

**Tech Stack:** Python, SimPy, NumPy, SQLite, PySide6, PyInstaller.

## Tasks and evidence

- [x] 1. Add `tests/test_aging.py`: six fixed analytic oracles from the approved spec, zero risk and cancellation-stability checks. Run `.venv/Scripts/python.exe -m pytest tests/test_aging.py -q` and confirm missing implementation fails. Implement `simlab/aging.py` functions `remaining_age(age, shape, scale, budget, multiplier)` and `risk_increment(age, elapsed, shape, scale, multiplier)`. Run the tests again.
- [x] 2. Add invalid-input and compiled-configuration tests using existing sample projects. Extend `extensions.py`, `compiler.py`, `schema.py` with `SimLabItemAging`; reject parent LRU, conflicting FRT, nonfinite values, invalid repair mode and unsafe event scales. Add Chinese labels and format 9 compatibility tests.
- [x] 3. Add deterministic real SimPy tests for actual running age, UTIL, pauses and horizon settlement. Extend `Components` with per-leaf age and lifetime hours, persistent risk budgets and enabled-path scheduling. Keep legacy output and random call order unchanged without aging.
- [x] 4. Add repair tests for PERFECT/MINIMAL, healthy siblings, replacement stock and transport. Connect `engine.py` and `maintenance.py` to actual completion updates and per-event records. Test direct LRU and layered models and flight abort returns.
- [x] 5. Add result/export tests. Implement Chinese age tables, all-replication CSV with provenance and explicit truncation. Test historical results and package roundtrip.
- [x] 6. Add mixed inspection/aging scenario and deterministic age conservation checks. Run full pytest, legacy case comparisons and source desktop smoke; inspect screenshots.
- [x] 7. Generate PERFECT/MINIMAL cases and reports. Build using `tools/build_windows.ps1` into staging. Run packaged smoke and source/package/stored-result equality.
- [x] 8. Install verified build at existing launcher path, preserve prior build and projects, verify installed smoke and ZIP CRC. Update docs and Obsidian. Audit all ten spec acceptance items before claiming M2 complete.

## First analytic test contract

```python
assert remaining_age(60, 2, 100, .64, 1) == pytest.approx(40)
assert risk_increment(60, 40, 2, 100, 1) == pytest.approx(.64)
```

Expected first run: missing `simlab.aging`. Expected after implementation: all analytic tests pass. These expectations are independent fixed values from the reviewed spec; tests must not compute expected values through production helpers.

2026-09-15：实现与验证完成，细项证据见docs/VERIFICATION.md。
