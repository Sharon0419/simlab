# SimLab 架构与交付复验 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于已有实现交付可运行的 SimLab 第一版及与源码一致的架构分析。

**Architecture:** 保留 Qt 桌面、独立 Python/SimPy 计算进程与 SQLite 项目结构。以现有测试和桌面检查确认交付，不重复搭建已有模块。

**Tech Stack:** Python、PySide6、SimPy、NumPy、SQLite、pytest、PyInstaller。

本环境未提供上述 superpowers 执行技能；本计划直接在当前会话按用户授权执行，不依赖这些技能。

- [x] 阅读 README、核心模块及已有交付记录，对照官方产品定位确认范围。
- [x] 执行 `.venv/Scripts/python.exe -m pytest -q`，要求全部通过；实际 14 passed。
- [x] 设置 `QT_QPA_PLATFORM=offscreen`，分别执行源码和便携版 `--smoke build/qa-20260907-source` / `--smoke build/qa-20260907-package`，要求退出 0 且结果 passed。
- [x] 新增 `docs/ARCHITECTURE.md`，记录模块责任、模型、计算口径、边界与扩展验收要求。
- [x] 检查实际结果截图，更新 README 和验证记录，复制架构说明至便携版，重新生成 ZIP；49.2 MB，完整性检查通过。
