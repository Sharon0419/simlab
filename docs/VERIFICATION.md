# 首版本机交付验证

## 深蓝主题更新（2026-09-07）

统一调整背景、面板、按钮、表格、字段说明和自绘图表颜色。重新构建 Windows 可执行版，运行 `--smoke build/qa-deep-blue-package`，退出码 0，八项桌面检查 passed；合成示例可用度仍为 0.9596487296178109。已检查建模与结果截图的中文和深蓝配色。

## 2026-09-07 复验

本次重新执行核心测试，结果为 **14 passed in 1.74s**。源码及现有 Windows 便携版分别执行端到端检查，均退出 0、状态 passed，八项检查全部通过。证据位于 `build/qa-20260907-source/smoke-result.json` 与 `build/qa-20260907-package/smoke-result.json`；本次合成示例的平均可用度为 0.9596487296178109，与两种运行方式一致。下文为首次构建记录。

验证日期：2026-09-04。软件版本：0.1.0。

## 字段基线

依据本机 SIMLOX 2017 安装目录内 DatabaseDictionary_SIMLOX.txt 生成。133 张表、891 个字段的代码、顺序、说明、类型、单位、默认值、约束与关联均逐项比对一致；源文件 SHA256 与 schema.json 中记录一致。

## 核心测试

命令：`.venv/Scripts/python.exe -m pytest -q`

结果：**14 passed**。

覆盖：字典数量与关键单位、示例引用关系、每百万小时故障率换算、非法数值、悬空引用与重复键、未支持参数拦截、SQLite 往返与覆盖备份、项目包导入分支、路径穿越与篡改拒绝、模型包不含结果、可复现、零故障、部件守恒、无备件单机解析基准、不可满足的资源组合拒绝。

## 桌面端到端验证

源码版：`build/qa-source-3/smoke-result.json`。

最终便携版：`dist/SimLab/SimLab.exe --smoke build/qa-release`，进程退出码 **0**，结果 **passed**。

通过八项检查：字段编辑、保存重开、CSV 导出、后台取消、后台计算、结果持久化、项目包导出导入、分支追溯。

样本为修改动力模块 FRT=1300 后的 20 台设备、90 天、10 次重复试验。源码版与便携版的平均可用度均为 **0.9596487296178109**。此数值来自合成数据，不代表真实装备指标。

完整流程截图位于 `build/qa-release/01-overview.png`、`02-modeling.png`、`03-results.png`，并复制模型与结果预览至本目录。已检查中文渲染、导航、表头、字段说明、结果卡片、曲线与停机分析表。

## 交付边界

已实现本机基础保障仿真与全量字段编辑。未声称复现原厂引擎；高级业务规则和原厂 .sxi 二进制文件尚不支持，界面与 README 已明确说明。仅在本机 Windows 11 完成执行验证。

打包时发现 PATH 上的 ICU 78 被错误收集，与 Qt 需要的 Windows ICU 未版本化入口不匹配；构建脚本已排除该库，并直接对修正后的最终可执行程序完成以上测试。
